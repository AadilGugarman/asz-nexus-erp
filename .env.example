# Copy this to .env and fill in values
# Vite exposes only VITE_* prefixed vars to the browser bundle

# Base URL for any external REST API (leave empty for pure Tauri/offline app)
VITE_API_BASE_URL=
